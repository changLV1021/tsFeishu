2020/12/17 提供发送的消息类型选择，目前支持text和markdown，其中text可以@人，但消息内容无格式，markdown格式更清晰标题更突出，但只能伪@（即有@字样但不会真的钉某个人）
2020/4/7 修复不配group就查不到内容的BUG

2020/4/7 TS系统2020年4月初改版，把这里返回系统用户名单的接口，返回数据类型改成了列表，所以对应修改

19年12月14日更新
适应ts的更新，增加了ts在需求查询和修改单查询页面新增的一些字段，能够抓取和推送这些字段

19年5月5日更新
1、解决有时会报“获取统一认证地址失败”错误的问题
2、应没有java开发环境的同学要求，还是提供编译好的版本，反正看起来公司版本库空间也不紧张

11月2日更新
1、处理cas改版后html页面bug导致的解析失败
2、支持将产品ID翻译为产品名称
3、支持一个任务多个负责人的情况下，翻译每个负责人的员工号为姓名
4、不再提供编译好的版本，因为太占体积了，有需要的请自行下载编译

5月20更新
不需要配置group了，只需要配param。另外，支持无限多组条件。注意：不需要的条件需要用#注释掉。

5月17日更新
1、因为上周末TS系统很奇怪的做了一个更新，在恒生统一登录后，增加了一层跳转，并且在所有后台接口调用中增加了一个公共参数testingag，导致ts2dingding 0.7版无法正常使用了（0.6版由于是模拟浏览器访问的，还可以继续用）。现应对了此修改，修复无法使用的问题。

5月9日更新
1、修复登录获取cas地址失败
2、修复总是重复调用ts系统参数接口和用户列表接口
3、修复不能只配需求、任务、修改单中的一种，必须都配的问题
4、修复日志不输出的问题


5月8日更新
1、增加了读取TS系统字典的功能，用于翻译一些变量值，例如需求状态、修改单状态等
2、增加了读取TS系统用户的功能，获取到的员工号和名字的对应关系，用于翻译接口返回的员工号为姓名

v0.7
1、从原本的selenium模拟浏览器人工操作的方式爬TS数据，改为直接调用HTTP/JSON后台接口直接获取数据，性能极大幅度提升。
2、配置方式简化，无需先在TS建模板，直接复制TS的查询条件即可
3、现在允许在同一个小工具中分别配置3个需求查询、3个任务查询、3个修改单查询，并且每个查询可推送内容到不同的钉钉群
4、需求、任务、修改单允许指定不同的人员列表
5、每个查询条件均可以指定不同的产品组。
6、删除了不常用的推送内容前后修饰语自定义等功能，简化代码
7、注意新版本不能向下兼容，原来的配置文件不可再用


PS.感谢黄泽鑫同学提供的TS后台接口调用代码。